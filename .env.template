# Copy this file to .env.local and fill in the values
# DO NOT commit .env.local to git!

# For OpenAI Vision API (Required for journal image processing)
OPENAI_API_KEY=your-openai-api-key

# Supabase Configuration (Required)
# Get these from your Supabase project dashboard > Settings > API
NEXT_PUBLIC_SUPABASE_URL=your-project-url # Example: https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key # Project API keys > anon public
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key # Project API keys > service_role

# Development Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000 # Change in production

# 100ms Video SDK Configuration (Required for video calls)
# Get these from https://dashboard.100ms.live/
# 1. Create account at 100ms.live
# 2. Create a new app/template
# 3. Go to Developer > App Access Key to get credentials
HMS_ACCESS_KEY=your-100ms-access-key
HMS_SECRET=your-100ms-secret-key
HMS_TEMPLATE_ID=your-100ms-template-id  # Template ID from your 100ms app
NEXT_PUBLIC_100MS_APP_ID=your-100ms-app-id  # App ID (visible to client)

# Stripe Configuration (Required for payments)
# Get these from https://dashboard.stripe.com/
# 1. Create account at stripe.com
# 2. Go to Developers > API keys
STRIPE_SECRET_KEY=sk_test_...                      # Secret key (server-side only)
STRIPE_WEBHOOK_SECRET=whsec_...                    # Webhook signing secret (from Stripe webhook settings)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...    # Publishable key (visible to client)

# Stripe Price IDs (create these products in Stripe Dashboard > Products)
# Challenge: $997 one-time payment
NEXT_PUBLIC_STRIPE_CHALLENGE_PRICE_ID=price_...

# Journal: $39.95/month subscription
NEXT_PUBLIC_STRIPE_JOURNAL_PRICE_ID=price_...

# Journal Annual: $399/year subscription (saves $80)
NEXT_PUBLIC_STRIPE_JOURNAL_ANNUAL_PRICE_ID=price_...

# Email Configuration (Required for transactional emails)
# Get API key from https://resend.com/api-keys
RESEND_API_KEY=re_...
EMAIL_FROM=Gynergy <hello@yourdomain.com>
EMAIL_REPLY_TO=support@yourdomain.com

# Application URL (for email links)
NEXT_PUBLIC_APP_URL=https://gynergy.app

# 100ms Webinar Configuration (Optional — only if using separate webinar template)
HMS_WEBINAR_TEMPLATE_ID=               # Webinar-specific template ID (falls back to HMS_TEMPLATE_ID)
HMS_WEBHOOK_SECRET=                    # HMAC secret for verifying 100ms webhook signatures

# Webinar Cron Secret (for securing cron endpoints)
CRON_SECRET=your-cron-secret